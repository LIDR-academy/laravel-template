# 03a — Session 2: Greenfield Tags CRUD (governance ON)

Run on top of the refactored blog. The prompt is only the WHAT; the rest is governed by
`CLAUDE.md` + `.claude/`.

```
/feature Build a full CRUD API for Tags.
```

> Watch: from reading the code alone, does it infer the schema (name + unique slug),
> auto-generate the slug, set public reads / authenticated writes (mirroring Posts), replace
> the inline `GET /tags`, and cover the full matrix with full-contract assertions?
