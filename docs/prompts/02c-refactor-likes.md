# 02c — Session 1: Refactor Likes (governance ON)

Run after Comments. The prompt is only the WHAT; the rest is governed by `CLAUDE.md` + `.claude/`.

```
/feature Refactor the Likes endpoint currently in routes/api.php into the target architecture.
```

> Watch: does it characterize the toggle contract (liked + likes_count), use a LikeService,
> add NO policy (any authenticated user may like), and leave Posts/Comments untouched?
